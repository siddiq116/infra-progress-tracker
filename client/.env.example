# Only needed for local dev when the client and API run as two separate
# servers (e.g. `npm run dev:server` + `npm run dev:client`). Leave unset in
# production on Vercel — the client and /api functions share one origin.
VITE_API_URL=http://localhost:5000
